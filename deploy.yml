- name: Build
  run: bun run build
  env:
    PUBLIC_SHEET_ID: ${{ secrets.PUBLIC_SHEET_ID }}
    PUBLIC_ADMIN_PASSWORD: ${{ secrets.PUBLIC_ADMIN_PASSWORD }}
    PUBLIC_APPS_SCRIPT_URL: ${{ secrets.PUBLIC_APPS_SCRIPT_URL }}
